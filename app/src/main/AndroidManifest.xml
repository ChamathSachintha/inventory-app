<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Internet (for API / trending products later) -->
    <uses-permission android:name="android.permission.INTERNET"/>

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.InventoryApp"
        android:exported="true">

        <!-- MAIN ENTRY -->
        <activity
            android:name=".MainActivity"
            android:exported="true">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>

        </activity>

        <!-- INVENTORY -->
        <activity
            android:name=".activities.InventoryActivity"
            android:exported="false"/>

        <!-- ADD / EDIT -->
        <activity
            android:name=".activities.AddEditProductActivity"
            android:exported="false"/>

        <!-- API / CURRENCY / TRENDING PAGE -->
        <activity
            android:name=".activities.SmartHubActivity"
            android:exported="false"/>

    </application>

</manifest>